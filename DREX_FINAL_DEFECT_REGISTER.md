# DREX V2 — FINAL CLOSURE DEFECT REGISTER
========================================
**Master Defect Census, Root Cause Analysis, & Verification Ledger**

- **Project**: DREX V2 — Integrated Secure Data Erasure & Advanced File Recovery Tool
- **SIH Problem Statement**: SIH26149 / PS149
- **Repository**: `D:\drex-v2-main`
- **Target Commit**: `f030382`
- **Audit Date**: 2026-09-16

---

## Master Defect Ledger (P0-01 to P0-25)

| Defect ID | Category | Location | Root Cause | Severity | Impact | Planned Resolution | Verification Test |
|---|---|---|---|---|---|---|---|
| **P0-01** | Case Context & Isolation | `drex_server.py:1388, 1934` | Overly aggressive rejection when `case_id` is omitted (`None`) breaks backwards compatibility for legacy suites; `case_id: ""` must be rejected with 400, but omitted `case_id` must bind to active/triage case. | Critical (P0) | 4 automated tests failed (`test_phase10`, `test_phase12`, `test_phase15`). | Differentiate omitted `case_id is None` (bind to active case) from invalid `case_id: ""` or non-existent cases (fail-closed HTTP 400). | `test_phase22_p0_01_case_binding.py`, `test_phase10_final_validation.py` |
| **P0-02** | Recovery Target Type Safety | `recovery_adapter.py:80-103, 260` | Passing `RecoveryTarget` object where `str` or `Path` was expected can trigger `AttributeError: 'RecoveryTarget' object has no attribute 'startswith'`. | High (P0) | Potential crash during raw sector recovery or adapter dispatch. | Add `__str__`, `__fspath__`, and `startswith` methods to `RecoveryTarget`; update adapter `validate_source` to handle `RecoveryTarget`. | Unit tests for `RecoveryTarget` duck typing and adapter dispatch. |
| **P0-03** | Stale Recovery Candidates | `webui/app.js:5822` | Previous scan candidate table was not immediately cleared when investigator initiated a new scan. | Medium (P0) | UI presented stale artifacts while new scan was in progress. | In `triggerRecoveryScan`, immediately clear `STATE.recoveryCandidates` and show scanning placeholder. | Browser E2E scan verification. |
| **P0-04** | File/Folder State Lifecycle | `webui/app.js:3371-3377` | Switching radio button between FILE and FOLDER retained old target path and stale inspection card. | Medium (P0) | Inconsistent target metadata displayed after type toggle. | Add `switchShredTargetType(newType)` to clear target input, phrase, inspection card, and update preflight. | Manual and unit testing of target switching. |
| **P0-05** | Device Deduplication | `drex_server.py:1035` | Multiple logical partitions (e.g. `C:\` and `D:\`) mapping to `\\.\PHYSICALDRIVE0` caused duplicate physical entries in `/api/devices`. | Medium (P0) | Redundant device cards and confusing qualification dropdowns. | Deduplicate physical drive descriptors by canonical `device_path` in `list_devices`. | `GET /api/devices` test asserting unique physical drive paths. |
| **P0-06** | System Drive Fail-Closed | `drex_server.py:1914` | Destructive operations on active OS boot/system media must fail closed with HTTP 422. | Critical (P0) | Potential catastrophic data loss if OS drive is wiped. | Enforce `DeviceIntelligenceEngine.is_system_drive` tripwire across all destructive routes. | `test_destructive_sanitization_strict_phrase_and_system_disk` |
| **P0-07** | System Drive UI Disabled State | `webui/app.js:3428` | Preflight must disable execute button and display clear OS protection warning when system target is selected. | High (P0) | Operator confusion if execute is clickable but fails. | Verify `btnEl.disabled = true` and badge shows `EXECUTION_DISABLED (OS System Protected)`. | UI preflight unit tests and browser E2E. |
| **P0-08** | Audit Ledger Sequence Integrity | `forensic_vault.py:1200+` | Audit ledger events must strictly preserve SHA-256 Merkle/hash-chaining with monotonically increasing sequence numbers. | Critical (P0) | Tampering or out-of-order events invalidate forensic chain of custody. | Enforce append-only mutex locking and cryptographic `prev_hash` chaining. | `tests/test_audit_ledger.py`, `tests/test_phase10_final_validation.py` |
| **P0-09** | Certificate Lifecycle & Verification | `certificate_engine.py:120+` | Certificates must decouple generation from verification; verification must recalculate hash from disk bytes. | High (P0) | Tampered certificates could be falsely attested as valid. | `drex_verify.py` standalone verification independent of server session. | `tests/test_forensic_certificate.py` |
| **P0-10** | Authentication & RBAC Hardening | `drex_rbac.py:80+` | Missing, expired, or tampered JWTs must fail closed with HTTP 401; unauthorized routes must return 403. | High (P0) | Privilege escalation or unauthenticated access to forensic operations. | Comprehensive RBAC persona matrix and constant-time token validation. | `test_complete_rbac_permission_matrix` |
| **P0-11** | Real Telemetry & Zero Fake Progress | `drex_server.py:1480, 2000+`, `file_sanitizer.py` | Overwrite and recovery progress must be strictly derived from physical I/O byte counters. | Critical (P0) | Violation of forensic truthfulness and SIH competition rules. | Implement streaming `progress_callback` in `FileSanitizer` and real `JobRegistry` updates. | `tests/test_phase22_gate3_telemetry.py` |
| **P0-12** | 0.01% Progress Floor Rule | `drex_server.py:2020+`, `webui/app.js` | 0 bytes written must show null/indeterminate; 1+ bytes written must show positive percentage (minimum 0.01%). | High (P0) | User perception of progress before work begins, or 0% display during active work. | Enforce `0.01%` floor on initial non-zero byte progress in server and UI card. | `tests/test_phase22_progress_truth.py`, `tests/test_js_progress_truth.js` |
| **P0-13** | Verification Phase Decoupling | `drex_server.py:2252+`, `webui/app.js` | Writing phase completion must never automatically attest verification success; verification is a separate phase. | High (P0) | Premature success badge before readback verification completes. | Separate `phase="VERIFYING"` and `verification_state="VERIFIED"` transitions. | `tests/test_phase22_progress_truth.py` |
| **P0-14** | Cooperative Cancellation | `file_sanitizer.py:245`, `drex_server.py` | Cancellation token must be checked per write chunk; status must transition to `CANCELLING` -> `CANCELLED`/`PARTIAL`. | High (P0) | Thread hangs or orphaned background workers continuing destructive writes. | `threading.Event` checked in inner loops; buffers flushed; zero false success certificate. | `tests/test_phase22_gate3_telemetry.py` |
| **P0-15** | Recovery E2E Execution | `recovery_adapter.py:600+` | Real multi-format recovery from real test disk images and buffers without simulated stubs. | High (P0) | Inability to recover deleted forensic artifacts in field triage. | Native carving and TSK integration with authentic candidate extraction. | `test_phase12_advanced_recovery.py` |
| **P0-16** | Fragment Reconstruction Adversarial Proof | `fragment_engine.py:400+` | Out-of-order reassembly must reject overlapping, truncated, or cyclic extents. | High (P0) | Corrupted reassembly output accepted as valid evidence. | Enforce strict boundary seam hashing, entropy continuity, and overlap rejection. | `tests/test_phase16_fragments.py` |
| **P0-17** | Evidence Vault Multi-Case Isolation | `forensic_vault.py:300+` | Evidence items must be strictly partitioned by `case_id`; cross-case leakage must be blocked. | Critical (P0) | Cross-case contamination in multi-tenant forensic workstations. | Scoped query filtering; missing `case_id` fails closed. | `tests/test_phase22_p0_01_case_binding.py` |
| **P0-18** | Certificate Tamper Evident Verification | `drex_verify.py:200+` | Byte-level modification of certificate PDF or manifest must immediately invalidate cryptographic verification. | High (P0) | Undetected tampering of forensic proof certificates. | Dual SHA-256 hashing of manifest and document body with Merkle root validation. | `tests/test_forensic_certificate.py` |
| **P0-19** | Test Recorder Authenticity | `scripts/generate_test_results.py` | `--collect-only` dry run previously marked all tests `PASSED` with 0.04s duration. | Critical (P0) | High-severity test provenance risk; false sense of test pass rates. | Rewrite to hook `pytest_runtest_logreport`; fail closed on `--collect-only`. | `PHASE22_TEST_PROVENANCE.md` |
| **P0-20** | Controlled Failure Detection | `scripts/generate_test_results.py` | Test recorder must accurately capture real test failures, errors, and tracebacks into JSON. | High (P0) | Undetected test regressions masking production bugs. | Verify intentional `assert False` produces `FAILED` status, duration, and traceback. | Verification gate test. |
| **P0-21** | Stale Build & Version Contradictions | `webui/index.html`, `webui/app.js`, `README.md` | Multiple UI locations hardcode `fbad09d` (3 commits behind HEAD `f030382`); README claims 106 tests. | High (P0) | Inconsistent version strings, confusing audit trails, stale documentation. | Replace hardcoded HTML/JS build tags with dynamic API fetch; update README. | Browser inspection of build tags and README review. |
| **P0-22** | Browser Workstation E2E Invariants | `tests/test_phase15_browser_e2e.py` | All 26 views and navigation workflows must remain error-free and connected to live backend APIs. | High (P0) | UI regressions or broken route transitions in web workstation. | Comprehensive 44-test browser E2E test suite. | `pytest tests/test_phase15_browser_e2e.py` |
| **P0-23** | Stale Validation Data Fallback | `webui/app.js:741-766` | Hardcoded fallback in `renderSystemValidation` claims 949 tests and commit `fbad09d`. | High (P0) | Offline or failed API call displays stale, contradictory data. | Remove hardcoded fallback; show authentic loading/error state if API is unreachable. | UI inspection of System Validation page. |
| **P0-24** | Path Traversal & Shell Injection Defenses | `drex_server.py:900+` | Target paths and export filenames must be sanitized to prevent directory traversal outside case boundaries. | High (P0) | Arbitrary file read/overwrite vulnerabilities on host filesystem. | `target_normalizer.py` path sanitization and strict validation. | `tests/test_hardware_safety_and_locking.py` |
| **P0-25** | Service Worker Cache Tagging | `webui/sw.js:4` | Cache name hardcodes `drex-v2-shell-fbad09d`. | Medium (P0) | Stale UI assets cached across browser reloads. | Update cache key to track authoritative release version. | Service worker lifecycle audit. |
